// https://react-native-elements.github.io/react-native-elements/docs/customization
// https://github.com/react-native-elements/react-native-elements/tree/next/src/config
export const theme = {
  Button: {
    raised: true,
  },
  colors: {
    deejay: '#f21f0c',
  },
};

export const storageKey = 'dj-download-dates';

// dev
export const highlightBorder = (color) => ({borderColor: color, borderWidth: 1});
